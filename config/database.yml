default: &default
  primary:
    database: rails611
    username: root
    password:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    host: 127.0.0.1
  primary_replica:
    database: rails611
    username: root
    password:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    host: 127.0.0.1
    replica: true

development: 
  <<: *default

test: 
  <<: *default

production: 
  <<: *default
